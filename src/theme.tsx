import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  fontSizes: {
    notificationHead: "16px",
    notificationBody: "12px"
  },
  colors: {
    darkPurple: "#929"
  }
});
